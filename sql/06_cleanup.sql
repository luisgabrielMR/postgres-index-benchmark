-- Safe to run multiple times.
DROP INDEX IF EXISTS idx_orders_customer_status_created_at;

-- Uncomment the line below if you also want to remove the benchmark table.
-- DROP TABLE IF EXISTS orders;
